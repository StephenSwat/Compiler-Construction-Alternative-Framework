#pragma once
#include <stdlib.h>
#include <stdio.h>
#include "types.h"

extern char *infile;
extern FILE *outfile;
extern char *currentfile;

extern int line;
extern int col;

extern backend_t backend;
